{
  "name": "@medesign/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "medesign MCP server — the tool surface agents drive the design loop through. Consumes @medesign/backend and wraps its API as MCP tools.",
  "main": "dist/index.js",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@medesign/backend": "*",
    "@medesign/graph": "*",
    "@medesign/vision-critic": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
