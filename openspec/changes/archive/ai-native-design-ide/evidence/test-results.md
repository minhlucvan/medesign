# Test Results — ai-native-design-ide

## vitest workspace (graph package)

11/11 tests passed — all graph, impact, and consistency tests green.

## vitest workspace (all packages)

159/180 tests passed (21 pre-existing failures):
- 16 design-md-parser tests (module not built — pre-existing)
- 5 integration tests (need running backend — pre-existing)

No new test failures introduced by this change.
