{
  "name": "@medesign/plugin-css",
  "version": "0.0.0",
  "type": "module",
  "description": "medesign CSS plugin — owns CSS->graph parsing (tokens, themes, colors) + new node types (breakpoint, mediaQuery, cssVarGroup, contrastPair) + CSS production-readiness doctor rules.",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc -p tsconfig.json" },
  "dependencies": { "@medesign/plugin-api": "*", "@medesign/graph": "*", "@medesign/dsr": "*" },
  "devDependencies": { "@types/node": "^20.14.0", "typescript": "^5.5.0" }
}
