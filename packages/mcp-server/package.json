{
  "name": "@emdesign/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "emdesign MCP server — the tool surface agents drive the design loop through. Consumes @emdesign/backend and wraps its API as MCP tools.",
  "main": "dist/index.js",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@emdesign/backend": "*",
    "@emdesign/graph": "*",
    "@emdesign/vision-critic": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
