{
  "name": "@medesign/plugin-api",
  "version": "0.0.0",
  "type": "module",
  "description": "medesign plugin contract — the capability-typed MedesignPlugin interface + hook types that framework/styling/library plugins implement. A leaf consumed by backend and every @medesign/plugin-*.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": { "@medesign/dsr": "*", "@medesign/graph": "*" },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
