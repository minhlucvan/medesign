{
  "name": "@medesign/graph",
  "version": "0.0.0",
  "type": "module",
  "description": "Labeled property graph that encodes any information of a design system — files, stories, components, tokens, colors, fonts, specs, rules, themes, artifacts — for craft, fix, impact analysis, and consistency.",
  "main": "dist/index.js",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ts-morph": "^23.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
